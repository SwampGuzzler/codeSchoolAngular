
var app = angular.module("store", []);
/* Where app is the name of the application, angular references the
Angular.js library, .module creates a new module, 'store' is the name of the application, and
the empty array are the app's dependencies, of which there are 
currently none. */

